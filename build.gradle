// Top-level build file.
// Plugin and dependency management is handled via settings.gradle (pluginManagement)
// and app/build.gradle's buildscript block, following the canonical ATAK plugin template.

task clean(type: Delete) {
    delete rootProject.buildDir
}

